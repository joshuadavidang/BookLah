import { Command, CommandInput } from "@/components/ui/command";

export default function ConcertSearch() {
  return (
    <Command>
      <CommandInput placeholder="Search for a concert" />
    </Command>
  );
}
